# Extract the data for the benchmark

Extracts the data needed to run the benchmark models and optionally
creates the study population.

## Usage

``` r
extractBenchmarkData(benchmarkDesign, createStudyPopulation = TRUE)
```

## Arguments

- benchmarkDesign:

  An object of class `benchmarkDesign`.

- createStudyPopulation:

  Whether to create the study population or not.
