# View the benchmark tasks

Loads the benchmark tasks to be run on an OMOP-CDM.

## Usage

``` r
viewBenchmarkTasks(tasks = "all")
```

## Arguments

- tasks:

  Define which tasks should be loaded. Choice of "all" to load all tasks
  or a numeric vector to define which.
